// use babel-register for coverage and test on raw source

before(function () {

  global.assert = require('chai').assert;
  global.should = require('chai').should();
  global.expect = require('chai').expect;

});
